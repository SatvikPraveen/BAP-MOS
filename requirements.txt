# BAPMOS — core training, preprocessing, inference, and smoke tests
#
# From the BAPMOS/ directory (no pyproject / editable install)::
#
#   pip install -r requirements.txt
#   export PYTHONPATH="$(pwd)/src${PYTHONPATH:+:$PYTHONPATH}"
#   python -m bapmos.method.bap_mos_trainer --help
#
# Or use ``./scripts/bapmos.sh …`` (sets PYTHONPATH for you).
#
# Recommended Python: 3.10 or 3.11 (3.9 works; 3.13 may lag for some wheels).
#
# PyTorch / CUDA:
#   Default PyPI wheels are often CPU-only. On GPU clusters, install torch +
#   torchvision first for YOUR CUDA version, then install the rest, e.g.:
#   https://pytorch.org/get-started/locally/
#   Example (replace cuXXX with your cluster)::
#     pip install torch==2.8.0 torchvision==0.23.0 \
#       --index-url https://download.pytorch.org/whl/cu124
#     pip install -r requirements.txt
#
# Optional stacks (install after this file)::
#   pip install -r requirements_dev.txt       # pytest (+ notebook extras)
#   pip install -r requirements-hpo.txt       # Optuna outer-loop search
#   See requirements_nnunet.txt               # nnU-Net v2 (separate env recommended)
#   CPU-only workstation: requirements_cpu.txt
#
# Pins keep training/inference environments reproducible across installs.
# This file lists packages the BAPMOS tree actually imports — not a full
# transitive freeze of unused Jupyter/Plotly stacks.

# --- deep learning / SAM ---
torch==2.8.0
torchvision==0.23.0
segment-anything==1.0
segmentation_models_pytorch==0.5.0
timm==1.0.22
safetensors==0.7.0

# --- numerics / images ---
numpy==2.0.2
scipy==1.13.1
opencv-python-headless==4.12.0.88
pillow==11.3.0
scikit-image==0.24.0
matplotlib==3.9.4
imageio==2.37.2
tifffile==2024.8.30

# --- data / configs / logging ---
PyYAML==6.0.3
pandas==2.3.3
pydicom==2.4.4
tqdm==4.67.1
wandb==0.23.1

# --- common transitive pins ---
filelock==3.19.1
fsspec==2025.10.0
networkx==3.2.1
sympy==1.14.0
typing_extensions==4.15.0
requests==2.32.5
protobuf==6.33.2
psutil==7.2.1
packaging==25.0
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3
contourpy==1.3.0
cycler==0.12.1
fonttools==4.60.2
kiwisolver==1.4.7
pyparsing==3.3.1
Jinja2==3.1.6
MarkupSafe==3.0.3
gitdb==4.0.12
GitPython==3.1.45
sentry-sdk==2.48.0
smmap==5.0.2
urllib3==2.6.2
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
click==8.1.8
platformdirs==4.4.0
