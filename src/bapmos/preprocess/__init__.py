"""Preprocessing entrypoints for prostate (pooled), bladder (PFUS1), and delineation QA.

Subpackages
-----------
- ``bapmos.preprocess.prostate`` — pooled corpus (``python -m bapmos.preprocess.prostate``)
- ``bapmos.preprocess.bladder`` — PFUS1 convert / splits / verify
- ``bapmos.preprocess.delineation`` — RTSTRUCT/RTPLAN overlays and organ-presence reports

See ``docs/PREPROCESS.md``.
"""
