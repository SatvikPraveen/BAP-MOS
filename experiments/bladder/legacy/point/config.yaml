# Legacy CE+Dice, fixed-ring, MSD-only protocol — see experiments/bladder/legacy/README.md
# (and experiments/prostate/legacy/README.md for the full protocol note).
# Historical only: kept runnable for curiosity; not required for main BAP-MOS tables.
# Bladder — point-only (PFUS1).
# Defaults: configs/bladder/common.yaml (objective_organ: Bladder)
# Train via: python -m bapmos.multiorgan.train_sam_multiorgan_decoder_points

inherits: ../../../configs/bladder/common.yaml
prompt_type: point
run_root: runs/bladder/legacy/point
wandb_project: bapmos_bladder_point
