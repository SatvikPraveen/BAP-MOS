# Legacy CE+Dice, fixed-ring, MSD-only protocol — see experiments/prostate/legacy/README.md.
# Historical only: kept runnable for curiosity; not required for main BAP-MOS tables.
# Prostate — point-only prompt baseline.
# Train via: python -m bapmos.multiorgan.train_sam_multiorgan_decoder_points

inherits: ../../../configs/prostate/common.yaml

prompt_type: point
run_root: runs/prostate/legacy/point
wandb_project: bapmos_prostate_point
