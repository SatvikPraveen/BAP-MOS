# Legacy CE+Dice, fixed-ring, MSD-only protocol — see experiments/prostate/legacy/README.md.
# Historical only: kept runnable for curiosity; not required for main BAP-MOS tables.
# Train via: python -m bapmos.legacy.optimization.trainer --config <this> --experiment <name>
# Box vs Point prompt ratios (mutually exclusive per sample).
# Canonical set: 10/90, 50/50, 90/10 only.
wandb_project: bapmos_prostate_box_point

common:
  data_root: data/prostate/pooled
  splits_subdir: splits_stratified
  sam_checkpoint: models/sam_base/sam_vit_b_01ec64.pth
  max_epochs: 300
  batch_size: 1
  lr: 1.0e-4
  seed: 42
  patience: 40
  num_pos_per_organ: 1
  num_neg_points: 3
  ring_width: 20
  num_workers: 4
  pixel_spacing: [0.159072, 0.159072]
  checkpoint_objective:
    metric: ptv_kfold_msd
    kfold_n: 5
    kfold_seed: 42
  checkpoint_metric: ptv_kfold_msd
  checkpoint_mode: min
  run_test_after_train: false
  run_root: runs/prostate/legacy/box_point

experiments:
  - name: box10_point90
    box_ratio: 0.1
    point_ratio: 0.9
  - name: box50_point50
    box_ratio: 0.5
    point_ratio: 0.5
  - name: box90_point10
    box_ratio: 0.9
    point_ratio: 0.1
